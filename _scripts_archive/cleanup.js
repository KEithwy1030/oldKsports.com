console.log('清理无效时间戳数据...'); const posts = JSON.parse(localStorage.getItem('oldksports_forum_posts') || '[]'); const cleanedPosts = posts.map(p => { if (!p.timestamp || isNaN(new Date(p.timestamp).getTime())) { p.timestamp = new Date().toISOString(); } return p; }); localStorage.setItem('oldksports_forum_posts', JSON.stringify(cleanedPosts)); console.log('清理完成，共处理', posts.length, '个帖子');
